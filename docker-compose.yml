services:

  db_dev:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    # set shared memory limit when using docker-compose
    shm_size: 128mb
    volumes:
      - dev_data:/var/lib/postgresql/data
      - .:/tmp
    environment:
      POSTGRES_HOST_AUTH_METHOD: trust
      POSTGRES_DB: tt_migration
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    networks:
      - tt-database-network

  db_qa:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    # set shared memory limit when using docker-compose
    shm_size: 128mb
    volumes:
      - qa_data:/var/lib/postgresql/data
      - .:/tmp
    environment:
      POSTGRES_HOST_AUTH_METHOD: trust
      POSTGRES_DB: tt_migration
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    networks:
      - tt-database-network

  db_snap:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    # set shared memory limit when using docker-compose
    shm_size: 128mb
    volumes:
      - snap_data:/var/lib/postgresql/data
      - .:/tmp
    environment:
      POSTGRES_HOST_AUTH_METHOD: trust
      POSTGRES_DB: tt_migration
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    networks:
      - tt-database-network

  db_prod:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    # set shared memory limit when using docker-compose
    shm_size: 128mb
    volumes:
      - prod_data:/var/lib/postgresql/data
      - .:/tmp
    environment:
      POSTGRES_HOST_AUTH_METHOD: trust
      POSTGRES_DB: tt_migration
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    networks:
      - tt-database-network

  redis:
    image: "redis:7.2"
    command: [ "redis-server","--appendonly","yes","--appendfsync","everysec" ]
    volumes:
      - /var/lib/yourapp/redis:/data
    restart: unless-stopped
    healthcheck:
      test: [ "CMD","redis-cli","ping" ]
      interval: 10s
      timeout: 3s
      retries: 20
    ports:
      - "6379:6379"
    networks:
      - tt-database-network

  redis-dev:
    build:
      context: .
      dockerfile: redis-dev/Dockerfile
      args:
        SSH_KEY_FILE: ${REDIS_DEV_SSH_KEY_PATH}
    environment:
      - BASTION_URL=${BASTION_URL}
      - REDIS_DEV_USER=${REDIS_DEV_USER}
      - REDIS_DEV_URL=${REDIS_DEV_URL}
    ports:
      - "6380:6378"
    restart: unless-stopped
    networks:
      - tt-database-network

  redis-insight:
    image: redis/redisinsight:latest
    restart: unless-stopped
    ports:
      - "5540:5540"
    volumes:
      - redis_insight_data:/data
    networks:
      - tt-database-network
    depends_on:
      redis:
        condition: service_healthy

  db-proxy:
    image: haproxy:2.9-alpine
    container_name: db-proxy
    restart: always
    ports:
      - "5432:5432"
    command: ["haproxy", "-f", "/usr/local/etc/haproxy/haproxy.cfg", "-f", "/usr/local/etc/haproxy/haproxy-frontend.cfg"]
    volumes:
      - ./haproxy/haproxy.cfg:/usr/local/etc/haproxy/haproxy.cfg:ro
      - ./haproxy/haproxy-frontend.cfg:/usr/local/etc/haproxy/haproxy-frontend.cfg:ro
      - ./get_current_env.sh:/tmp/get_current_env.sh:ro
    networks:
      - tt-database-network
    depends_on:
      - db_dev
      - db_qa
      - db_snap
      - db_prod

volumes:
  dev_data:
  qa_data:
  snap_data:
  prod_data:
  redis_insight_data:

networks:
  tt-database-network:
    